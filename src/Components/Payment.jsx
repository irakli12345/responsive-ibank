
function Payment() {
    return (
        <div className="payment">
            <button>Spend Money </button>
        </div>
    )
}

export default Payment
/* use state variable showPayment, and setShowPayment to toggle the form 

it should ask: Description, the type of expense, amount, which creditcard, assign it unique ID and capture date. */